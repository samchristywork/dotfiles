data.location=~/.local/share/task
hooks.location=~/.config/task/hooks

news.version=2.6.0

# Simple User Defined Attribute
uda.estimate.type=string
uda.estimate.label=Size Estimate
uda.estimate.values=huge,large,medium,small,trivial,

# task show report.list:
# report.list.columns     id,start.age,entry.age,depends.indicator,priority,project,tags,recur.indicator,scheduled.countdown,due,until.remaining,description.count,urgency
# report.list.context     1
# report.list.description Most details of tasks
# report.list.filter      status:pending -WAITING
# report.list.labels      ID,Active,Age,D,P,Project,Tags,R,Sch,Due,Until,Description,Urg
# report.list.sort        start-,due+,project+,urgency-

report.t.columns     = id,start.age,entry.age,depends.indicator,priority,project,tags,recur.indicator,scheduled.countdown,due,until.remaining,due.relative,description.count,urgency
report.t.context     = 1
report.t.description = Customized report based on report.list
report.t.filter      = status:pending -WAITING
report.t.labels      = ID,Active,Age,D,P,Project,Tags,R,Sch,Due,Until,Relative,Description,Urg
report.t.sort        = urgency+
